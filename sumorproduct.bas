Print "Weird program"
Print "Try running this program twice, putting same values once and different values the next time."
Print "Enter 2 numbers"
Input a
Input b
If a = b Then
    Print a + b
Else
    Print a * b
end if
